<nav class="bg-gray-800 text-white px-6 py-4 flex justify-between items-center shadow">
  <a href="{{ path('home') }}" class="font-bold text-xl hover:text-gray-300 transition">VersusBrawl</a>

  <div class="space-x-4 text-sm">
    {% if app.user %}
      <span>Connecté en tant que <strong>{{ app.user.username }}</strong></span>
      <a href="{{ path('app_team_new') }}" class="hover:underline">Créer une équipe</a>

      {% if is_granted('ROLE_EVENT_ADMIN') or is_granted('ROLE_SUPER_ADMIN') %}
        <a href="{{ path('app_event_manage') }}" class="hover:underline text-purple-300">Gérer mes événements</a>
      {% endif %}

      <a href="{{ path('app_logout') }}" class="hover:underline text-red-400">Déconnexion</a>
    {% else %}
      <a href="{{ path('app_login') }}" class="hover:underline">Connexion</a>
      <a href="{{ path('app_register') }}" class="hover:underline">Inscription</a>
    {% endif %}
  </div>
</nav>
